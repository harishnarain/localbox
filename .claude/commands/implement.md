---
description: Work through a spec's task checklist, checking items off as they land — stage 4 of specify → plan → tasks → implement
argument-hint: <issue-number>
---

Use the `spec-manager` agent, Stage 4 (Implement), on issue $ARGUMENTS.
