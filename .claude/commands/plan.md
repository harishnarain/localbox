---
description: Draft the technical plan for a spec — stage 2 of specify → plan → tasks → implement
argument-hint: <issue-number>
---

Use the `spec-manager` agent, Stage 2 (Plan), on issue $ARGUMENTS.
