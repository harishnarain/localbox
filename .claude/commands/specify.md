---
description: Create a new spec as a tracked GitHub Issue — stage 1 of specify → plan → tasks → implement
argument-hint: <short feature description>
---

Use the `spec-manager` agent, Stage 1 (Specify), for: $ARGUMENTS

If the description doesn't give you enough to write concrete acceptance
criteria, ask before inventing them.
