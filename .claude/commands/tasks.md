---
description: Break a spec's plan into an ordered, trackable task checklist — stage 3 of specify → plan → tasks → implement
argument-hint: <issue-number>
---

Use the `spec-manager` agent, Stage 3 (Tasks), on issue $ARGUMENTS.
